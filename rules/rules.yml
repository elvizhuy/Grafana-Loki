groups:
  - name: rate-alerting
    rules:
      - alert: HighLogRate
        expr: sum by(job) (rate({job="log-app-ndtp-dev"} [1m]))
        for: 1m
        labels:
            severity: warning
            category: logs
        annotations:
            title: "Alert log"
            description: "có log"